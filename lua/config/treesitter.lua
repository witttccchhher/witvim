return {
  ensure_installed = { "lua", "luadoc", "printf", "vim", "vimdoc", "python", "nix", "html", "css", "javascript", "typescript" },

  highlight = {
    enable = true,
    use_languagetree = true
  },

  indent = { enable = true }
}
